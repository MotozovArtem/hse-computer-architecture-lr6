enum Routes {
    HOME = "/",
    MANUAL = "/manual",
    LRU = "/lru",
    LFU = "/lfu",
    ARC = "/arc",
    CAR = "/car",
    RND = "/rnd",
    FIFO = "/fifo",
    MRU = "/mru"
}

export default Routes;